import { HoverBookmarkDirective } from './hover-bookmark.directive';

describe('HoverBookmarkDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBookmarkDirective();
    expect(directive).toBeTruthy();
  });
});
